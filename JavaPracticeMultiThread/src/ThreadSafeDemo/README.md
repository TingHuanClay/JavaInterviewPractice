ArrayList is not an Thread-Safe Collection
let's take it as an example to demonstrate when would make Thread-unsafe

Some case of NOT Thread-Safe collections:
ArrayList、LinkedList、Map、HashMap、Set、HashSet